import { Request, Response, NextFunction } from 'express';
import { Connect, Query } from '../config/mysql';

//insert Into User_Activities(Day, User_ID, Activities_ID) Values (CurDate(), UserIdInput, ActivitiesIdInput)
//select preciso perguntar como vai ser o dash board, se calcular por atividades por dia ou por tempo no dia
export default {  };